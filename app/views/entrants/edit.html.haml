%h2
  Editing Entrant

.well{ :class => 'well-small' }
  = link_to "Back to Entrants", entrants_path, :class => 'btn'
  = link_to "Delete", entrant_path(@entrant), :method => :delete, :class => 'btn btn-danger'
  = link_to "Add Another", new_entrant_path, :class => 'btn btn-primary'

= render 'form'
